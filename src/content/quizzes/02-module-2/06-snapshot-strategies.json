{
  "lessonSlug": "02-module-2/06-snapshot-strategies",
  "moduleId": "02-module-2",
  "_meta": {
    "idPrefix": "m02-06",
    "description": "Use IDs: m02-06-q1, m02-06-q2, ... for MC/TF; m02-06-code-1 for code challenges"
  },
  "questions": [
    {
      "id": "m02-06-q1",
      "type": "multiple-choice",
      "question": "Зачем при первом запуске нового Debezium-коннектора необходим initial snapshot?",
      "options": [
        { "id": "a", "text": "Snapshot создаёт резервную копию базы данных для восстановления при сбое коннектора" },
        { "id": "b", "text": "CDC захватывает только изменения (INSERT/UPDATE/DELETE), поэтому без snapshot существующие данные в таблицах останутся невидимыми для consumer — snapshot передаёт текущее состояние таблиц в Kafka" },
        { "id": "c", "text": "Snapshot необходим для создания слота репликации и настройки Publication на стороне PostgreSQL" },
        { "id": "d", "text": "Snapshot проверяет схему таблиц и генерирует Avro-схемы для Schema Registry" }
      ],
      "correctAnswer": "b",
      "explanation": "CDC по своей природе захватывает только изменения данных — INSERT, UPDATE, DELETE, происходящие после создания слота репликации. Записи, которые уже существуют в таблице, не генерируют CDC-событий. Initial snapshot решает эту проблему: при первом запуске коннектор читает текущее состояние всех отслеживаемых таблиц и публикует каждую существующую строку как событие с `op: 'r'` (read). Без snapshot consumer увидит только новые изменения, а миллионы существующих записей останутся невидимыми. Вариант C ошибочен — слот репликации создаётся независимо от режима snapshot.",
      "bloomLevel": "conceptual"
    },
    {
      "id": "m02-06-q2",
      "type": "multiple-choice",
      "question": "Коннектор сконфигурирован с `snapshot.mode=always`. После каждого рестарта все данные отправляются в Kafka повторно. Почему это анти-паттерн для production-окружения?",
      "options": [
        { "id": "a", "text": "Режим always не поддерживается в PostgreSQL — он работает только с MySQL коннектором" },
        { "id": "b", "text": "Каждый рестарт вызывает полный snapshot: streaming блокируется на время снэпшота, все данные дублируются в Kafka, WAL retention растёт из-за накопления непрочитанных сегментов" },
        { "id": "c", "text": "Режим always пересоздаёт слот репликации при каждом запуске, что приводит к потере оффсетов" },
        { "id": "d", "text": "Режим always несовместим с incremental snapshots и signaling table" }
      ],
      "correctAnswer": "b",
      "explanation": "Режим `snapshot.mode=always` запускает полный традиционный snapshot при каждом старте коннектора. Это создаёт три проблемы в production: (1) streaming полностью блокируется на время snapshot — новые CDC-события накапливаются в слоте репликации, увеличивая WAL retention и рискуя заполнить диск; (2) все существующие данные дублируются в Kafka при каждом рестарте, создавая огромный объём избыточных событий; (3) для больших таблиц snapshot может занимать часы, в течение которых реальные изменения не обрабатываются. Этот режим допустим только для разработки и тестирования.",
      "bloomLevel": "applied"
    },
    {
      "id": "m02-06-q3",
      "type": "multiple-choice",
      "question": "Таблица имеет средний размер строки 10KB. Целевой объём памяти для одного chunk incremental snapshot — 50MB. Какой chunk size следует установить в конфигурации `incremental.snapshot.chunk.size`?",
      "options": [
        { "id": "a", "text": "500 строк — формула: 50MB / 10KB = 5000, делим на 10 для запаса" },
        { "id": "b", "text": "5120 строк — формула: (target_memory_MB * 1024) / avg_row_size_KB = (50 * 1024) / 10 = 5120" },
        { "id": "c", "text": "50000 строк — формула: target_memory_MB * 1000 = 50 * 1000" },
        { "id": "d", "text": "1024 строк — стандартное значение, не зависящее от размера строки" }
      ],
      "correctAnswer": "b",
      "explanation": "Формула расчёта chunk size: (target_chunk_memory_MB * 1024) / average_row_size_KB. Подставляя значения: (50 * 1024) / 10 = 51200 / 10 = 5120 строк. Каждый chunk будет загружать примерно 5120 строк по 10KB, что займёт около 50MB памяти. Вариант A применяет неправильную формулу и затем добавляет произвольный делитель. Вариант D ошибочен — chunk size необходимо рассчитывать на основе фактического размера строки, иначе слишком большие chunk'и приведут к OutOfMemory, а слишком маленькие — к избыточным запросам к базе.",
      "bloomLevel": "applied"
    },
    {
      "id": "m02-06-q4",
      "type": "multiple-choice",
      "question": "Команде необходимо выполнить snapshot таблицы размером 500GB в рабочее время без прерывания реального CDC-streaming. Какой подход следует использовать и почему традиционный snapshot неприменим?",
      "options": [
        { "id": "a", "text": "Традиционный snapshot с `snapshot.mode=initial` — он достаточно быстр для 500GB при настройке параллельных потоков" },
        { "id": "b", "text": "Incremental snapshot через signaling table: он читает данные порциями (chunks), параллельно обрабатывая streaming-события, возобновляем при сбое и не блокирует CDC-поток" },
        { "id": "c", "text": "Запуск `snapshot.mode=when_needed` — этот режим автоматически выбирает между традиционным и incremental snapshot в зависимости от размера таблицы" },
        { "id": "d", "text": "Экспорт данных через pg_dump и загрузка в Kafka через отдельный producer, чтобы не нагружать коннектор" }
      ],
      "correctAnswer": "b",
      "explanation": "Incremental snapshot — единственный подход, отвечающий всем требованиям. Традиционный snapshot (вариант A) блокирует streaming на всё время выполнения: для 500GB это могут быть часы, в течение которых CDC-события накапливаются в слоте репликации, увеличивая WAL retention и создавая риск заполнения диска. Кроме того, традиционный snapshot не возобновляем — при сбое придётся начинать заново. Incremental snapshot работает порциями (chunks) по primary key, обрабатывая streaming-события параллельно. При сбое он возобновляется с последнего chunk благодаря watermark tracking. Режим `when_needed` (вариант C) не переключается автоматически на incremental — он просто запускает традиционный snapshot, если offset потерян.",
      "hint": "Сравните два подхода по трём критериям: блокировка streaming, возобновляемость при сбое и влияние на WAL retention. Подумайте, что произойдёт с CDC-потоком, если snapshot 500GB таблицы займёт несколько часов.",
      "bloomLevel": "analytical"
    }
  ]
}
